# TEST FIXPRICE_SCRAPY
1. install libs

```
python -m venv env
source env/bin/activate
pip install -r requirements.txt
``` 
or with poetry
```
poetry install
```
2. scrapy scrapy scrapy scrapy scrapy
```
scrapy crawl fixprice
```
